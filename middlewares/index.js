const notFound = require('./notFound'),
      errorHandler = require('./errorHandler'),
      isAuthorized = require('./isAuthorized');


module.exports = {
  notFound,
  errorHandler,
  isAuthorized
}
